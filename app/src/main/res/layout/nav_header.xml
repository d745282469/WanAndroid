<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="190dp"
    android:background="@color/colorPrimary"
    android:padding="@dimen/page_padding">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/slide_header_icon"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:src="@mipmap/ic_launcher"
        android:layout_centerInParent="true"/>

    <TextView
        android:id="@+id/slide_header_nickName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="@dimen/nav_username_text_size"
        android:textColor="@color/page_text_white"
        android:layout_above="@id/slide_header_userName"/>

    <TextView
        android:id="@+id/slide_header_userName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/nav_header_username_default"
        android:textSize="@dimen/nav_username_text_size"
        android:textColor="@color/page_text_white"
        android:layout_alignParentBottom="true"/>

</RelativeLayout>